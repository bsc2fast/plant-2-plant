{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Serve on LAN (for phone)",
      "type": "node-terminal",
      "request": "launch",
      "command": "echo \"\\nMobile URL: http://$(ipconfig getifaddr en0 || ipconfig getifaddr en1):4450\\nLocal URL:  http://localhost:4450\\n\" && python3 -m http.server 4450",
      "cwd": "${workspaceFolder}"
    },
    {
      "name": "Launch Chrome (server must be running)",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:4450",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
